{
  "name": "@weatherx/model-scheduler",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "npm run types && tsc --noEmit && vitest run && npm run test:workflow && wrangler deploy --dry-run",
    "test": "vitest run",
    "test:workflow": "node test-bake-workflow.mjs && node test-schedule-contract.mjs && node test-production-cutover-contract.mjs && node test-ui-release-contract.mjs",
    "types": "wrangler types --env-interface CloudflareBindings --include-runtime false",
    "verify:live": "node scripts/verify-live-schedules.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.126.0"
  },
  "engines": {
    "node": ">=22"
  }
}
