# Workflow inventory

Generated by `node tools/workflow-inventory.mjs --write`. Do not edit this file by hand.

This is a navigation index of **declared configuration**, not a release or recovery menu. Workflow YAML and guarded helpers remain the execution authority. Expressions are shown without evaluation. Observed activation, deployed scheduler state, last execution, and last publication are **unknown** here. A source pin or a recurring label does not prove that a workflow is enabled or deployed.

Metadata describes purpose and support intent only. `legacy-needs-review` entries are not recommended recovery paths. Historical runbook narratives do not override the linked executable declarations. See [operation and recovery guidance](WORKFLOW_OPERATIONS.md).

Source digest (registry and workflow bytes): `f849e4f7ee6dfca89f3757c62982f382262a45e56b53156edf7a0d34d1379ea2`.

51 workflows. Ordering and output are deterministic; no API request or clock is used.

| Workflow | Family / subsystem | Lifecycle | Purpose | Runbook |
| --- | --- | --- | --- | --- |
| [bake](#bake) | models / Model collection | recurring | Collect independent model inputs and assemble the whole-maintenance fallback. | [guide](../docs/single-model-refresh.md) |
| [catalog-bake](#catalog-bake) | models / Component freshness | recurring | Refresh individual core model components from provider schedules or a manual request. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-bootstrap](#catalog-bootstrap) | models / Catalog bootstrap | manual-supported | Initialize the staging catalog through its guarded bootstrap path. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-production-bootstrap](#catalog-production-bootstrap) | models / Catalog bootstrap | manual-supported | Initialize the production catalog through its guarded bootstrap path. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-production-safety-drill](#catalog-production-safety-drill) | models / Catalog safety | diagnostic | Exercise production catalog safety only through the explicit guarded drill. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-promote-existing](#catalog-promote-existing) | models / Publication recovery | legacy-needs-review | Legacy staging promotion recovery requires source-pin and writer-lock review before use. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-r2-probe](#catalog-r2-probe) | models / Storage boundaries | diagnostic | Probe scoped staging component storage access. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [catalog-safety-drill](#catalog-safety-drill) | models / Catalog safety | diagnostic | Exercise staging catalog concurrency and rollback under the existing guarded drill. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [collect-core-model](#collect-core-model) | models / Model collection | recurring | Collect and seal one core model for the calling bake. | [guide](../docs/current-model-artifact-handoff.md) |
| [collect-regional-model](#collect-regional-model) | models / Model collection | recurring | Collect one regional model or retain an honest abstention receipt. | [guide](../docs/current-model-artifact-handoff.md) |
| [consumer-refresh](#consumer-refresh) | releases / Production consumers | manual-supported | Refresh production consumers with guarded verification and rollback. | [guide](../CONSUMER_REFRESH.md) |
| [data-edge-deploy](#data-edge-deploy) | releases / Production data edge | manual-supported | Deploy the selected production data-edge phase under its explicit guards. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [data-reader-refresh](#data-reader-refresh) | releases / Production data reader | manual-supported | Refresh the production data reader with compatible fallback recovery. | [guide](../docs/data-reader-refresh.md) |
| [fusion-evaluate](#fusion-evaluate) | maintenance / Fusion evaluation | manual-supported | Evaluate retained fusion inputs through the existing guarded evaluation path. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [fusion-infra](#fusion-infra) | control-plane / Fusion infrastructure | manual-supported | Provision the selected fusion infrastructure through explicit deployment guards. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [fusion-issue](#fusion-issue) | maintenance / Fusion issuance | recurring | Issue the selected fusion candidate under existing qualification rules. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [fusion-promote](#fusion-promote) | maintenance / Fusion promotion | manual-supported | Promote a qualified fusion candidate through the guarded controller. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [gdacs-feed-release](#gdacs-feed-release) | releases / Hazard feed | manual-supported | Release the isolated GDACS route through guarded verification and recovery. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [hydrology](#hydrology) | archives / Hydrology | manual-supported | Prepare hydrology artifacts through the explicit staging path. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [model-inputs](#model-inputs) | staging / Experimental models | manual-supported | Collect isolated experimental model inputs with bounded provider concurrency. | [guide](../docs/MODEL_CLOUD_INPUTS.md) |
| [nam-hi-diagnostic](#nam-hi-diagnostic) | staging / Experimental models | diagnostic | Diagnose a bounded NAM-HI acquisition without broadening publication authority. | [guide](../docs/nam-hi-cloud-diagnostic.md) |
| [observation-bake](#observation-bake) | maintenance / Observation freshness | recurring | Refresh point observations through the existing serialized immutable whole-release publisher. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [point-route-activate](#point-route-activate) | releases / Production point routes | manual-supported | Activate reviewed point routes with ownership-aware rollback. | [guide](../docs/point-route-activation.md) |
| [publish-current-model-production](#publish-current-model-production) | models / Component publication | recurring | Qualify and publish one authenticated collector result independently of sibling models. | [guide](../docs/current-model-artifact-handoff.md) |
| [qualify-bake-throughput](#qualify-bake-throughput) | models / Bake qualification | diagnostic | Qualify full-grid GFS frame throughput and runner memory without provider or publication access. | [guide](../docs/bake-throughput-qualification.md) |
| [resume-model-publication](#resume-model-publication) | models / Publication recovery | manual-supported | Recover publication from the explicitly reviewed retained run without recollecting models. | [guide](../docs/resume-model-publication.md) |
| [satellite-archive](#satellite-archive) | archives / Satellite and radar | recurring | Collect hourly archive tails or explicit bounded historical batches. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [scheduler-ci](#scheduler-ci) | control-plane / Controller evidence | recurring | Check scheduler, publication, source, and recovery contracts. | [guide](../scheduler/README.md) |
| [scheduler-deploy](#scheduler-deploy) | control-plane / Scheduler | manual-supported | Deploy and verify the selected scheduler revision through existing guards. | [guide](../scheduler/README.md) |
| [source-checkout-probe](#source-checkout-probe) | control-plane / Source provenance | diagnostic | Probe exact source checkout isolation without granting deployment authority. | [guide](../docs/source-checkout-isolation.md) |
| [staging-consumer-refresh](#staging-consumer-refresh) | staging / Staging consumers | manual-supported | Refresh staging consumers with existing verification and restore controls. | [guide](../docs/staging-consumer-restore-20260905.md) |
| [staging-current-selection](#staging-current-selection) | staging / Staging selection | recurring | Record the current eligible selection for the staging controller. | [guide](../docs/CLOUD-STAGING.md) |
| [staging-data-activate](#staging-data-activate) | staging / Staging data | manual-supported | Activate qualified staging data within the existing publication transaction. | [guide](../docs/CLOUD-STAGING.md) |
| [staging-data](#staging-data) | staging / Staging data | manual-supported | Prepare shared staging data under existing source and isolation guards. | [guide](../docs/CLOUD-STAGING.md) |
| [staging-follow-master](#staging-follow-master) | staging / Staging selection | recurring | Follow qualified master changes through the staging controller. | [guide](../docs/staging-latency-controller.md) |
| [staging-model-components](#staging-model-components) | staging / Experimental models | manual-supported | Prepare isolated model components for staging qualification. | [guide](../docs/STAGING_MODEL_COMPONENTS.md) |
| [staging-model-selection](#staging-model-selection) | staging / Experimental models | manual-supported | Select the reviewed model components for a staging candidate. | [guide](../docs/STAGING_MODEL_COMPONENTS.md) |
| [staging-place-renewal](#staging-place-renewal) | staging / Places | manual-supported | Renew the reviewed staging place data through the existing controller. | [guide](../docs/staging-place-renewal-plan.md) |
| [staging-places](#staging-places) | staging / Places | manual-supported | Prepare and qualify the isolated staging place data. | [guide](../docs/staging-places.md) |
| [staging-search-reader](#staging-search-reader) | staging / Search | manual-supported | Activate the guarded staging search reader and verify its boundary. | [guide](../docs/staging-search-reader.md) |
| [staging-search](#staging-search) | staging / Search | manual-supported | Prepare isolated search data for staging qualification. | [guide](../docs/staging-search.md) |
| [staging-shared-read-pin](#staging-shared-read-pin) | staging / Shared data reading | manual-supported | Pin the reviewed shared-data selection for staging. | [guide](../docs/STAGING_SHARED_READ.md) |
| [staging-shared-read-probe](#staging-shared-read-probe) | staging / Shared data reading | diagnostic | Probe staging shared-data access without treating the probe as activation. | [guide](../docs/STAGING_SHARED_READ.md) |
| [staging-tc-guidance](#staging-tc-guidance) | staging / Tropical cyclone guidance | manual-supported | Prepare reviewed tropical cyclone guidance for isolated staging use. | [guide](../docs/STAGING_TC_GUIDANCE.md) |
| [staging-wind100-preflight](#staging-wind100-preflight) | staging / Native wind | diagnostic | Check recurring staging wind credentials through the existing check-only path. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [staging-wind100-recurring](#staging-wind100-recurring) | staging / Native wind | recurring | Maintain the isolated recurring staging native wind selection. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [staging-wind100](#staging-wind100) | staging / Native wind | manual-supported | Qualify isolated native 100 metre wind for staging. | [guide](../docs/WORKFLOW_OPERATIONS.md) |
| [ui-release](#ui-release) | releases / Application shell | manual-supported | Promote an eligible immutable application candidate through production guards. | [guide](../docs/production-safe-controller-20260907.md) |
| [ui-staging-tc](#ui-staging-tc) | staging / Application shell | manual-supported | Build an isolated staging shell with reviewed tropical cyclone guidance. | [guide](../docs/STAGING_TC_GUIDANCE.md) |
| [ui-staging](#ui-staging) | staging / Application shell | manual-supported | Build and qualify an immutable staging application candidate. | [guide](../docs/UI-STAGING-PROMOTION.md) |
| [verify-backfill](#verify-backfill) | maintenance / Verification archive | manual-supported | Backfill verification history with the existing bounded recovery controls. | [guide](../docs/WORKFLOW_OPERATIONS.md) |

## bake

[.github/workflows/bake.yml](../.github/workflows/bake.yml#L1) · <code>bake</code>

Declared triggers: [schedule](../.github/workflows/bake.yml#L10) <code>["30 2,8,14,20 * * *"]</code>; [workflow_dispatch](../.github/workflows/bake.yml#L12); input names <code>["model","recovery_run_id","staging_wind100_only"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [core-ecmwf](../.github/workflows/bake.yml#L31) ← no needs | <code>./.github/workflows/collect-core-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [staging-wind100](../.github/workflows/bake.yml#L37) ← <code>core-ecmwf</code> | <code>./.github/workflows/staging-wind100-recurring.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [core-gfs](../.github/workflows/bake.yml#L45) ← no needs | <code>./.github/workflows/collect-core-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [core-hrrr](../.github/workflows/bake.yml#L51) ← no needs | <code>./.github/workflows/collect-core-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [core-aifs](../.github/workflows/bake.yml#L57) ← no needs | <code>./.github/workflows/collect-core-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-icon](../.github/workflows/bake.yml#L63) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-hrdps](../.github/workflows/bake.yml#L69) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-arome-antilles](../.github/workflows/bake.yml#L75) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-hrrr-ak](../.github/workflows/bake.yml#L81) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-nam](../.github/workflows/bake.yml#L87) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-nam-hi](../.github/workflows/bake.yml#L93) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [regional-nam-ak](../.github/workflows/bake.yml#L99) ← no needs | <code>./.github/workflows/collect-regional-model.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-ecmwf](../.github/workflows/bake.yml#L105) ← <code>core-ecmwf</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-gfs](../.github/workflows/bake.yml#L116) ← <code>core-gfs</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-hrrr](../.github/workflows/bake.yml#L127) ← <code>core-hrrr</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-aifs](../.github/workflows/bake.yml#L138) ← <code>core-aifs</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-icon](../.github/workflows/bake.yml#L149) ← <code>regional-icon</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-hrdps](../.github/workflows/bake.yml#L160) ← <code>regional-hrdps</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-arome-antilles](../.github/workflows/bake.yml#L171) ← <code>regional-arome-antilles</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-hrrr-ak](../.github/workflows/bake.yml#L182) ← <code>regional-hrrr-ak</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-nam](../.github/workflows/bake.yml#L193) ← <code>regional-nam</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-nam-hi](../.github/workflows/bake.yml#L204) ← <code>regional-nam-hi</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [publish-nam-ak](../.github/workflows/bake.yml#L215) ← <code>regional-nam-ak</code> | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [component-publish-status](../.github/workflows/bake.yml#L226) ← <code>["publish-ecmwf","publish-gfs","publish-hrrr","publish-aifs","publish-icon","publish-hrdps","publish-arome-antilles","publish-hrrr-ak","publish-nam","publish-nam-hi","publish-nam-ak"]</code> | <code>ubuntu-latest</code> | not declared | <code>3</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [bake](../.github/workflows/bake.yml#L255) ← <code>["core-ecmwf","core-gfs","core-hrrr","core-aifs","regional-icon","regional-hrdps","regional-arome-antilles","regional-hrrr-ak","regional-nam","regional-nam-hi","regional-nam-ak"]</code> | <code>ubuntu-latest</code> | <code>production</code> | <code>300</code> | <code>{"group":"weatherx-data-maintenance","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |
| [model-status](../.github/workflows/bake.yml#L546) ← <code>["core-ecmwf","core-gfs","core-hrrr","core-aifs","regional-icon","regional-hrdps","regional-arome-antilles","regional-hrrr-ak","regional-nam","regional-nam-hi","regional-nam-ak","bake"]</code> | <code>ubuntu-latest</code> | not declared | <code>8</code> | no job group; workflow-wide limit still applies if declared | not declared | <code>{"contents":"read","actions":"read"}</code> |

Declared job conditions (additional step/helper checks may apply):

- [core-ecmwf](../.github/workflows/bake.yml#L33): <code>${{ (inputs.staging_wind100_only == true &amp;&amp; github.event_name == 'workflow_dispatch' &amp;&amp; inputs.model == 'ecmwf' &amp;&amp; inputs.recovery_run_id == '') &#124;&#124; (inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'ecmwf')) }}</code>
- [staging-wind100](../.github/workflows/bake.yml#L39): <code>${{ needs.core-ecmwf.result == 'success' &amp;&amp; (inputs.staging_wind100_only != true &#124;&#124; (github.event_name == 'workflow_dispatch' &amp;&amp; inputs.model == 'ecmwf' &amp;&amp; inputs.recovery_run_id == '')) }}</code>
- [core-gfs](../.github/workflows/bake.yml#L47): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'gfs') }}</code>
- [core-hrrr](../.github/workflows/bake.yml#L53): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'hrrr') }}</code>
- [core-aifs](../.github/workflows/bake.yml#L59): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'aifs') }}</code>
- [regional-icon](../.github/workflows/bake.yml#L65): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'icon') }}</code>
- [regional-hrdps](../.github/workflows/bake.yml#L71): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'hrdps') }}</code>
- [regional-arome-antilles](../.github/workflows/bake.yml#L77): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'arome-antilles') }}</code>
- [regional-hrrr-ak](../.github/workflows/bake.yml#L83): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'hrrr-ak') }}</code>
- [regional-nam](../.github/workflows/bake.yml#L89): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'nam') }}</code>
- [regional-nam-hi](../.github/workflows/bake.yml#L95): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'nam-hi') }}</code>
- [regional-nam-ak](../.github/workflows/bake.yml#L101): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (inputs.model == '' &#124;&#124; inputs.model == 'all' &#124;&#124; inputs.model == 'nam-ak') }}</code>
- [publish-ecmwf](../.github/workflows/bake.yml#L106): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.core-ecmwf.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-gfs](../.github/workflows/bake.yml#L117): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.core-gfs.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-hrrr](../.github/workflows/bake.yml#L128): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.core-hrrr.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-aifs](../.github/workflows/bake.yml#L139): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.core-aifs.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-icon](../.github/workflows/bake.yml#L150): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-icon.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-hrdps](../.github/workflows/bake.yml#L161): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-hrdps.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-arome-antilles](../.github/workflows/bake.yml#L172): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-arome-antilles.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-hrrr-ak](../.github/workflows/bake.yml#L183): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-hrrr-ak.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-nam](../.github/workflows/bake.yml#L194): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-nam.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-nam-hi](../.github/workflows/bake.yml#L205): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-nam-hi.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [publish-nam-ak](../.github/workflows/bake.yml#L216): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (needs.regional-nam-ak.result == 'success' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true') }}</code>
- [component-publish-status](../.github/workflows/bake.yml#L227): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (always()) }}</code>
- [bake](../.github/workflows/bake.yml#L266): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (always() &amp;&amp; !cancelled() &amp;&amp; needs.core-ecmwf.result == 'success' &amp;&amp; needs.core-gfs.result == 'success' &amp;&amp; needs.core-hrrr.result == 'success' &amp;&amp; needs.core-aifs.result == 'success' &amp;&amp; (inputs.recovery_run_id == '' &#124;&#124; (needs.regional-icon.result == 'success' &amp;&amp; needs.regional-hrdps.result == 'success' &amp;&amp; needs.regional-arome-antilles.result == 'success' &amp;&amp; needs.regional-hrrr-ak.result == 'success' &amp;&amp; needs.regional-nam.result == 'success' &amp;&amp; needs.regional-nam-hi.result == 'success' &amp;&amp; needs.regional-nam-ak.result == 'success'))) }}</code>
- [model-status](../.github/workflows/bake.yml#L547): <code>${{ inputs.staging_wind100_only != true &amp;&amp; (always()) }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [bake / checkout exact public bake diagnostic controller](../.github/workflows/bake.yml#L268) | <code>Andrewegao/v3t7kq-cycle</code> | <code>${{ github.sha }}</code> |
| [bake / checkout atmos (private, read-only deploy key)](../.github/workflows/bake.yml#L282) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |
| [bake / checkout this reviewed recovery transfer checker](../.github/workflows/bake.yml#L373) | <code>Andrewegao/v3t7kq-cycle</code> | <code>${{ github.sha }}</code> |
| [model-status / step 1](../.github/workflows/bake.yml#L554) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): [CURRENT_RUN_COMPONENT_PUBLISH_ENABLED](../.github/workflows/bake.yml#L106), [R2_FALLBACK_MIRROR_ENABLED](../.github/workflows/bake.yml#L503).


## catalog-bake

[.github/workflows/catalog-bake.yml](../.github/workflows/catalog-bake.yml#L1) · <code>WeatherX component bake</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-bake.yml#L6); input names <code>["model","target","bootstrap_missing"]</code>; [schedule](../.github/workflows/catalog-bake.yml#L27) <code>["8-59/10 * * * *","7 * * * *"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [model](../.github/workflows/catalog-bake.yml#L31) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>210</code> | <code>{"group":"weatherx-component-${{ github.event_name == 'workflow_dispatch' &amp;&amp; inputs.target &#124;&#124; vars.CATALOG_DEFAULT_TARGET &#124;&#124; 'staging' }}-${{ matrix.model }}","cancel-in-progress":false}</code> | <code>{"fail-fast":false,"max-parallel":4,"matrix":{"model":"${{ fromJSON(github.event_name == 'workflow_dispatch' &amp;&amp; inputs.model == 'slow' &amp;&amp; '[\"ecmwf\",\"gfs\",\"aifs\"]' &#124;&#124; github.event_name == 'workflow_dispatch' &amp;&amp; inputs.model != 'all' &amp;&amp; format('[\"{0}\"]', inputs.model) &#124;&#124; github.event_name == 'workflow_dispatch' &amp;&amp; '[\"ecmwf\",\"gfs\",\"hrrr\",\"aifs\"]' &#124;&#124; github.event.schedule == '8-59/10 * * * *' &amp;&amp; '[\"hrrr\"]' &#124;&#124; '[\"ecmwf\",\"gfs\",\"aifs\"]') }}"}}</code> | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [model](../.github/workflows/catalog-bake.yml#L34): <code>${{ github.event_name == 'workflow_dispatch' &#124;&#124; vars.CATALOG_GITHUB_FALLBACK_DISABLED != 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [model / Checkout WeatherX](../.github/workflows/catalog-bake.yml#L70) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |

Variable references (declared names only; values and activation unknown): [CATALOG_DEFAULT_TARGET](../.github/workflows/catalog-bake.yml#L36), [CATALOG_GITHUB_FALLBACK_DISABLED](../.github/workflows/catalog-bake.yml#L34), [CURRENT_RUN_COMPONENT_PUBLISH_ATMOS_SHA](../.github/workflows/catalog-bake.yml#L53), [CURRENT_RUN_POINT_REUSE_MODEL](../.github/workflows/catalog-bake.yml#L54).


## catalog-bootstrap

[.github/workflows/catalog-bootstrap.yml](../.github/workflows/catalog-bootstrap.yml#L1) · <code>WeatherX catalog bootstrap</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-bootstrap.yml#L5).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [staging](../.github/workflows/catalog-bootstrap.yml#L8) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>180</code> | <code>{"group":"weatherx-catalog-bootstrap-staging","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [staging / Checkout WeatherX](../.github/workflows/catalog-bootstrap.yml#L15) | <code>Andrewegao/atmos</code> | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## catalog-production-bootstrap

[.github/workflows/catalog-production-bootstrap.yml](../.github/workflows/catalog-production-bootstrap.yml#L1) · <code>WeatherX production catalog bootstrap</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-production-bootstrap.yml#L5); input names <code>["confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-catalog-bootstrap","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [bootstrap](../.github/workflows/catalog-production-bootstrap.yml#L20) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>210</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [bootstrap / Checkout the reviewed WeatherX publisher](../.github/workflows/catalog-production-bootstrap.yml#L28) | <code>Andrewegao/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): none detected.


## catalog-production-safety-drill

[.github/workflows/catalog-production-safety-drill.yml](../.github/workflows/catalog-production-safety-drill.yml#L1) · <code>WeatherX production catalog safety drill</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-production-safety-drill.yml#L5); input names <code>["confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-catalog-safety-drill","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [drill](../.github/workflows/catalog-production-safety-drill.yml#L20) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>15</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [drill / Checkout the scheduler control plane](../.github/workflows/catalog-production-safety-drill.yml#L28) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [drill / Checkout the reviewed WeatherX publisher](../.github/workflows/catalog-production-safety-drill.yml#L38) | <code>Andrewegao/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): [CATALOG_DEFAULT_TARGET](../.github/workflows/catalog-production-safety-drill.yml#L34).


## catalog-promote-existing

[.github/workflows/catalog-promote-existing.yml](../.github/workflows/catalog-promote-existing.yml#L1) · <code>WeatherX recover existing component promotion</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-promote-existing.yml#L6); input names <code>["model","artifact_id","expected_previous_manifest_sha256","expected_rollback_epoch"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [staging](../.github/workflows/catalog-promote-existing.yml#L28) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>10</code> | <code>{"group":"weatherx-component-${{ inputs.model }}","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [staging / Checkout WeatherX publisher](../.github/workflows/catalog-promote-existing.yml#L35) | <code>Andrewegao/atmos</code> | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## catalog-r2-probe

[.github/workflows/catalog-r2-probe.yml](../.github/workflows/catalog-r2-probe.yml#L1) · <code>WeatherX catalog R2 scope probe</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-r2-probe.yml#L5).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [probe](../.github/workflows/catalog-r2-probe.yml#L8) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [probe / Checkout WeatherX](../.github/workflows/catalog-r2-probe.yml#L12) | <code>Andrewegao/atmos</code> | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## catalog-safety-drill

[.github/workflows/catalog-safety-drill.yml](../.github/workflows/catalog-safety-drill.yml#L1) · <code>WeatherX staging catalog safety drill</code>

Declared triggers: [workflow_dispatch](../.github/workflows/catalog-safety-drill.yml#L6); input names <code>["confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [staging](../.github/workflows/catalog-safety-drill.yml#L13) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>15</code> | <code>{"group":"weatherx-catalog-safety-drill-staging","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [staging / Checkout WeatherX publisher](../.github/workflows/catalog-safety-drill.yml#L24) | <code>Andrewegao/atmos</code> | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## collect-core-model

[.github/workflows/collect-core-model.yml](../.github/workflows/collect-core-model.yml#L1) · <code>collect one core model</code>

Declared triggers: [workflow_call](../.github/workflows/collect-core-model.yml#L7); input names <code>["model","recovery_run_id"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [collector](../.github/workflows/collect-core-model.yml#L20) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>70</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [collector / checkout atmos core collector (same approved commit, read-only deploy key)](../.github/workflows/collect-core-model.yml#L35) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |
| [collector / checkout this reviewed recovery controller](../.github/workflows/collect-core-model.yml#L47) | <code>Andrewegao/v3t7kq-cycle</code> | <code>${{ github.sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## collect-regional-model

[.github/workflows/collect-regional-model.yml](../.github/workflows/collect-regional-model.yml#L1) · <code>collect one regional model</code>

Declared triggers: [workflow_call](../.github/workflows/collect-regional-model.yml#L7); input names <code>["model","recovery_run_id"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [collector](../.github/workflows/collect-regional-model.yml#L20) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>55</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [collector / checkout atmos regional collector (same approved commit, read-only deploy key)](../.github/workflows/collect-regional-model.yml#L35) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |
| [collector / checkout this reviewed recovery controller](../.github/workflows/collect-regional-model.yml#L47) | <code>Andrewegao/v3t7kq-cycle</code> | <code>${{ github.sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## consumer-refresh

[.github/workflows/consumer-refresh.yml](../.github/workflows/consumer-refresh.yml#L1) · <code>WeatherX production consumer refresh</code>

Declared triggers: [workflow_dispatch](../.github/workflows/consumer-refresh.yml#L7); input names <code>["atmosphere_sha","expected_release","confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-data-edge","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [consumer-refresh](../.github/workflows/consumer-refresh.yml#L31) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>50</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [consumer-refresh](../.github/workflows/consumer-refresh.yml#L31): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [consumer-refresh / Checkout the exact cycle workflow source](../.github/workflows/consumer-refresh.yml#L49) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [consumer-refresh / Checkout the exact reviewed Atmos source](../.github/workflows/consumer-refresh.yml#L56) | <code>Andrewegao/atmos</code> | <code>${{ inputs.atmosphere_sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## data-edge-deploy

[.github/workflows/data-edge-deploy.yml](../.github/workflows/data-edge-deploy.yml#L1) · <code>WeatherX production data edge</code>

Declared triggers: [workflow_dispatch](../.github/workflows/data-edge-deploy.yml#L5); input names <code>["phase","confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-data-edge","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [deploy](../.github/workflows/data-edge-deploy.yml#L25) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>20</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [deploy / Checkout the reviewed WeatherX release](../.github/workflows/data-edge-deploy.yml#L41) | <code>Andrewegao/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): none detected.


## data-reader-refresh

[.github/workflows/data-reader-refresh.yml](../.github/workflows/data-reader-refresh.yml#L1) · <code>WeatherX production data reader refresh</code>

Declared triggers: [workflow_dispatch](../.github/workflows/data-reader-refresh.yml#L5); input names <code>["atmosphere_sha","boundary_digest","confirm","exclusive_window"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-data-edge","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [data-reader-refresh](../.github/workflows/data-reader-refresh.yml#L29) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>40</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [data-reader-refresh](../.github/workflows/data-reader-refresh.yml#L29): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [data-reader-refresh / Checkout exact controller](../.github/workflows/data-reader-refresh.yml#L46) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [data-reader-refresh / Checkout exact reviewed source](../.github/workflows/data-reader-refresh.yml#L54) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.atmosphere_sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## fusion-evaluate

[.github/workflows/fusion-evaluate.yml](../.github/workflows/fusion-evaluate.yml#L1) · <code>WeatherX fusion evaluation</code>

Declared triggers: [workflow_dispatch](../.github/workflows/fusion-evaluate.yml#L4); input names <code>["mode","confirmation"]</code>; [schedule](../.github/workflows/fusion-evaluate.yml#L15) <code>["47 9 * * *"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-fusion-evaluation","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [evaluate](../.github/workflows/fusion-evaluate.yml#L24) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>180</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [evaluate](../.github/workflows/fusion-evaluate.yml#L24): <code>${{ github.ref == 'refs/heads/main' &amp;&amp; vars.FUSION_FEEDBACK_ENABLED == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [evaluate / step 1](../.github/workflows/fusion-evaluate.yml#L29) | <code>Andrewegao/atmos</code> | <code>${{ vars.FUSION_ENGINE_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [FUSION_CALIBRATION_RUNTIME_ENABLED](../.github/workflows/fusion-evaluate.yml#L64), [FUSION_ENGINE_SHA](../.github/workflows/fusion-evaluate.yml#L32), [FUSION_FEEDBACK_ENABLED](../.github/workflows/fusion-evaluate.yml#L24), [FUSION_SHADOW_CANDIDATE](../.github/workflows/fusion-evaluate.yml#L54), [FUSION_SHADOW_RUN](../.github/workflows/fusion-evaluate.yml#L50).


## fusion-infra

[.github/workflows/fusion-infra.yml](../.github/workflows/fusion-infra.yml#L1) · <code>WeatherX fusion infrastructure</code>

Declared triggers: [workflow_dispatch](../.github/workflows/fusion-infra.yml#L4); input names <code>["target","confirmation"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-fusion-infrastructure-${{ inputs.target }}","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [deploy](../.github/workflows/fusion-infra.yml#L21) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"${{ inputs.target }}"}</code> | <code>20</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [deploy](../.github/workflows/fusion-infra.yml#L21): <code>${{ github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [deploy / step 1](../.github/workflows/fusion-infra.yml#L27) | <code>Andrewegao/atmos</code> | <code>${{ vars.FUSION_ENGINE_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [FUSION_ENGINE_SHA](../.github/workflows/fusion-infra.yml#L30).


## fusion-issue

[.github/workflows/fusion-issue.yml](../.github/workflows/fusion-issue.yml#L1) · <code>WeatherX fusion issuance</code>

Declared triggers: [workflow_dispatch](../.github/workflows/fusion-issue.yml#L3); [schedule](../.github/workflows/fusion-issue.yml#L5) <code>["23 */6 * * *"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-fusion-issuance","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [record](../.github/workflows/fusion-issue.yml#L14) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>45</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [record](../.github/workflows/fusion-issue.yml#L14): <code>${{ github.ref == 'refs/heads/main' &amp;&amp; vars.FUSION_FEEDBACK_ENABLED == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [record / step 1](../.github/workflows/fusion-issue.yml#L19) | <code>Andrewegao/atmos</code> | <code>${{ vars.FUSION_ENGINE_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [FUSION_CALIBRATION_RUNTIME_ENABLED](../.github/workflows/fusion-issue.yml#L49), [FUSION_ENGINE_SHA](../.github/workflows/fusion-issue.yml#L22), [FUSION_FEEDBACK_ENABLED](../.github/workflows/fusion-issue.yml#L14), [FUSION_SHADOW_CANDIDATE](../.github/workflows/fusion-issue.yml#L44), [FUSION_SHADOW_RUN](../.github/workflows/fusion-issue.yml#L40).


## fusion-promote

[.github/workflows/fusion-promote.yml](../.github/workflows/fusion-promote.yml#L1) · <code>WeatherX fusion guarded promotion</code>

Declared triggers: [workflow_dispatch](../.github/workflows/fusion-promote.yml#L4); input names <code>["operation","evaluation_run","candidate_id","expected_calibration","rollback_target","confirmation"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-fusion-promotion","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [promote](../.github/workflows/fusion-promote.yml#L34) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>15</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [promote](../.github/workflows/fusion-promote.yml#L34): <code>${{ github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [promote / step 1](../.github/workflows/fusion-promote.yml#L39) | <code>Andrewegao/atmos</code> | <code>${{ vars.FUSION_ENGINE_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [FUSION_ENGINE_SHA](../.github/workflows/fusion-promote.yml#L42).


## gdacs-feed-release

[.github/workflows/gdacs-feed-release.yml](../.github/workflows/gdacs-feed-release.yml#L1) · <code>WeatherX guarded GDACS list repair</code>

Declared triggers: [workflow_dispatch](../.github/workflows/gdacs-feed-release.yml#L6); input names <code>["atmos_sha","expected_release","confirm"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-ui-production","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [repair](../.github/workflows/gdacs-feed-release.yml#L31) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [repair](../.github/workflows/gdacs-feed-release.yml#L31): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [repair / Checkout exact cycle workflow source](../.github/workflows/gdacs-feed-release.yml#L47) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [repair / Checkout exact reviewed Atmos source](../.github/workflows/gdacs-feed-release.yml#L53) | <code>Andrewegao/atmos</code> | <code>${{ inputs.atmos_sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## hydrology

[.github/workflows/hydrology.yml](../.github/workflows/hydrology.yml#L1) · <code>hydrology</code>

Declared triggers: [workflow_dispatch](../.github/workflows/hydrology.yml#L8); input names <code>["atmos_ref","run_bake_validation","limit"]</code>.

Workflow permissions: not declared. Workflow concurrency: <code>{"group":"hydrology","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [prepare](../.github/workflows/hydrology.yml#L30) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>330</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [prepare / Checkout atmos ref (private, read-only deploy key)](../.github/workflows/hydrology.yml#L34) | <code>Andrewegao/atmos</code> | <code>${{ inputs.atmos_ref }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## model-inputs

[.github/workflows/model-inputs.yml](../.github/workflows/model-inputs.yml#L1) · <code>WeatherX experimental model cloud inputs</code>

Declared triggers: [workflow_dispatch](../.github/workflows/model-inputs.yml#L6); input names <code>["source_sha","init"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-experimental-model-inputs","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [collect-independent](../.github/workflows/model-inputs.yml#L24) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>90</code> | no job group; workflow-wide limit still applies if declared | <code>{"fail-fast":false,"max-parallel":3,"matrix":{"model":["icon","hrdps","arome-antilles"]}}</code> | inherits workflow/default policy |
| [collect](../.github/workflows/model-inputs.yml#L100) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>120</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [collect-independent / step 1](../.github/workflows/model-inputs.yml#L44) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [collect-independent / Checkout exact approved additional-model collector](../.github/workflows/model-inputs.yml#L53) | <code>Andrewegao/atmos</code> | <code>${{ inputs.source_sha }}</code> |
| [collect / step 1](../.github/workflows/model-inputs.yml#L114) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [collect / Checkout exact approved additional-model collector](../.github/workflows/model-inputs.yml#L126) | <code>Andrewegao/atmos</code> | <code>${{ inputs.source_sha }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/model-inputs.yml#L35), [STAGING_MODEL_APPROVED_SOURCE_SHA](../.github/workflows/model-inputs.yml#L37), [STAGING_MODEL_COLLECTION_ENABLED](../.github/workflows/model-inputs.yml#L36), [STAGING_R2_ACCOUNT_ID](../.github/workflows/model-inputs.yml#L38).


## nam-hi-diagnostic

[.github/workflows/nam-hi-diagnostic.yml](../.github/workflows/nam-hi-diagnostic.yml#L1) · <code>WeatherX NAM-HI cloud diagnostic</code>

Declared triggers: [workflow_dispatch](../.github/workflows/nam-hi-diagnostic.yml#L5); input names <code>["init"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-nam-hi-diagnostic","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [diagnose](../.github/workflows/nam-hi-diagnostic.yml#L17) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [diagnose](../.github/workflows/nam-hi-diagnostic.yml#L17): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [diagnose / Checkout exact controller without retained credentials](../.github/workflows/nam-hi-diagnostic.yml#L24) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [diagnose / Checkout only the approved read-only scientific source](../.github/workflows/nam-hi-diagnostic.yml#L34) | <code>weatherx-hq/atmos</code> | <code>77487534a6ff0a17bf4e5d55f9ab5c06938138d4</code> |

Variable references (declared names only; values and activation unknown): none detected.


## observation-bake

[.github/workflows/observation-bake.yml](../.github/workflows/observation-bake.yml#L1) · <code>observation-bake</code>

Declared triggers: [schedule](../.github/workflows/observation-bake.yml#L9) <code>["10 2,8,14,20 * * *"]</code>; [workflow_dispatch](../.github/workflows/observation-bake.yml#L10).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [observation-points](../.github/workflows/observation-bake.yml#L14) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>60</code> | <code>{"group":"weatherx-data-maintenance","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [observation-points / checkout exact CI-approved Atmos observation source](../.github/workflows/observation-bake.yml#L21) | <code>weatherx-hq/atmos</code> | <code>14a2cda498ffb499cbd0fe2410d3818e364fe4d0</code> |
| [observation-points / checkout bounded Atmos master ancestry witness](../.github/workflows/observation-bake.yml#L40) | <code>weatherx-hq/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): none detected.


## point-route-activate

[.github/workflows/point-route-activate.yml](../.github/workflows/point-route-activate.yml#L1) · <code>WeatherX paired point route activation</code>

Declared triggers: [workflow_dispatch](../.github/workflows/point-route-activate.yml#L5); input names <code>["atmosphere_sha","data_version_id","script_etag","boundary_digest","confirm"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-production-data-edge","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [point-route](../.github/workflows/point-route-activate.yml#L33) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [point-route](../.github/workflows/point-route-activate.yml#L33): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [point-route / Checkout exact controller](../.github/workflows/point-route-activate.yml#L53) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [point-route / Checkout exact reviewed source](../.github/workflows/point-route-activate.yml#L59) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.atmosphere_sha }}</code> |

Variable references (declared names only; values and activation unknown): none detected.


## publish-current-model-production

[.github/workflows/publish-current-model-production.yml](../.github/workflows/publish-current-model-production.yml#L1) · <code>publish one current model to shared production data</code>

Declared triggers: [workflow_call](../.github/workflows/publish-current-model-production.yml#L7); input names <code>["model","kind","retained_run_id"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [publisher](../.github/workflows/publish-current-model-production.yml#L34) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>90</code> | <code>{"group":"weatherx-component-production-${{ inputs.model }}","cancel-in-progress":false}</code> | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [publisher / checkout authenticated current-run controller](../.github/workflows/publish-current-model-production.yml#L82) | <code>Andrewegao/v3t7kq-cycle</code> | <code>${{ github.sha }}</code> |
| [publisher / checkout exact qualified Atmos component publisher](../.github/workflows/publish-current-model-production.yml#L89) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |

Variable references (declared names only; values and activation unknown): [CURRENT_RUN_COMPONENT_PUBLISH_ATMOS_SHA](../.github/workflows/publish-current-model-production.yml#L53), [CURRENT_RUN_COMPONENT_PUBLISH_ENABLED](../.github/workflows/publish-current-model-production.yml#L52), [CURRENT_RUN_POINT_REUSE_MODEL](../.github/workflows/publish-current-model-production.yml#L73).


## qualify-bake-throughput

[.github/workflows/qualify-bake-throughput.yml](../.github/workflows/qualify-bake-throughput.yml#L1) · <code>WeatherX bake throughput qualification</code>

Declared triggers: [workflow_dispatch](../.github/workflows/qualify-bake-throughput.yml#L4).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-bake-throughput-qualification","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [qualify](../.github/workflows/qualify-bake-throughput.yml#L15) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"atmos-source-read-ui"}</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [qualify](../.github/workflows/qualify-bake-throughput.yml#L15): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [qualify / checkout exact qualification controller](../.github/workflows/qualify-bake-throughput.yml#L27) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [qualify / checkout reviewed production-source candidate](../.github/workflows/qualify-bake-throughput.yml#L33) | <code>weatherx-hq/atmos</code> | <code>0335a3b0a85b629c8659032a032bbc5ea0911eff</code> |

Variable references (declared names only; values and activation unknown): none detected.


## resume-model-publication

[.github/workflows/resume-model-publication.yml](../.github/workflows/resume-model-publication.yml#L1) · <code>Resume reviewed retained model publication</code>

Declared triggers: [workflow_dispatch](../.github/workflows/resume-model-publication.yml#L7); input names <code>["model"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [resume](../.github/workflows/resume-model-publication.yml#L16) ← no needs | <code>./.github/workflows/publish-current-model-production.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | <code>{"fail-fast":false,"max-parallel":11,"matrix":{"model":"${{ fromJSON(inputs.model == 'all' &amp;&amp; '[\"ecmwf\",\"gfs\",\"hrrr\",\"aifs\",\"icon\",\"hrdps\",\"arome-antilles\",\"hrrr-ak\",\"nam\",\"nam-hi\",\"nam-ak\"]' &#124;&#124; format('[\"{0}\"]', inputs.model)) }}"}}</code> | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [resume](../.github/workflows/resume-model-publication.yml#L16): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' &amp;&amp; vars.CURRENT_RUN_COMPONENT_PUBLISH_ENABLED == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| No direct checkout; inspect linked reusable jobs | — | — |

Variable references (declared names only; values and activation unknown): [CURRENT_RUN_COMPONENT_PUBLISH_ENABLED](../.github/workflows/resume-model-publication.yml#L16).


## satellite-archive

[.github/workflows/satellite-archive.yml](../.github/workflows/satellite-archive.yml#L1) · <code>satellite-archive</code>

Declared triggers: [schedule](../.github/workflows/satellite-archive.yml#L8) <code>["25 * * * *"]</code>; [workflow_dispatch](../.github/workflows/satellite-archive.yml#L10); input names <code>["policy","from","to"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"satellite-archive","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [hourly](../.github/workflows/satellite-archive.yml#L238) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"satellite-archive"}</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [backfill-plan](../.github/workflows/satellite-archive.yml#L350) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"satellite-archive"}</code> | <code>5</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [backfill](../.github/workflows/satellite-archive.yml#L406) ← <code>backfill-plan</code> | <code>ubuntu-latest</code> | <code>{"name":"satellite-archive"}</code> | <code>340</code> | no job group; workflow-wide limit still applies if declared | <code>{"fail-fast":false,"max-parallel":4,"matrix":{"month":"${{ fromJson(needs.backfill-plan.outputs.month_shards) }}"}}</code> | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [hourly](../.github/workflows/satellite-archive.yml#L238): <code>${{ github.ref == 'refs/heads/main' &amp;&amp; vars.SATELLITE_ARCHIVE_ENABLED == '1' &amp;&amp; (github.event_name == 'schedule' &#124;&#124; (github.event_name == 'workflow_dispatch' &amp;&amp; inputs.policy == 'hourly-tail-v1')) }}</code>
- [backfill-plan](../.github/workflows/satellite-archive.yml#L350): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' &amp;&amp; ((inputs.policy == 'storm-window-3d-v1' &amp;&amp; vars.SATELLITE_ARCHIVE_STORM_PILOT_ENABLED == '1') &#124;&#124; (inputs.policy == 'rolling-year-v1' &amp;&amp; vars.SATELLITE_ARCHIVE_ROLLING_YEAR_ENABLED == '1')) }}</code>
- [backfill](../.github/workflows/satellite-archive.yml#L406): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' &amp;&amp; ((inputs.policy == 'storm-window-3d-v1' &amp;&amp; vars.SATELLITE_ARCHIVE_STORM_PILOT_ENABLED == '1') &#124;&#124; (inputs.policy == 'rolling-year-v1' &amp;&amp; vars.SATELLITE_ARCHIVE_ROLLING_YEAR_ENABLED == '1')) }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [hourly / step 2](../.github/workflows/satellite-archive.yml#L252) | <code>weatherx-hq/atmos</code> | <code>${{ vars.SATELLITE_ARCHIVE_ATMOS_SHA }}</code> |
| [backfill-plan / step 2](../.github/workflows/satellite-archive.yml#L370) | <code>weatherx-hq/atmos</code> | <code>${{ vars.SATELLITE_ARCHIVE_ATMOS_SHA }}</code> |
| [backfill / step 1](../.github/workflows/satellite-archive.yml#L431) | <code>weatherx-hq/atmos</code> | <code>${{ needs.backfill-plan.outputs.atmos_sha }}</code> |

Variable references (declared names only; values and activation unknown): [R2_COST_PROJECTION_OBSERVED_AT](../.github/workflows/satellite-archive.yml#L267), [R2_PROJECTED_30D_CLASS_A_MILLION](../.github/workflows/satellite-archive.yml#L269), [R2_PROJECTED_30D_CLASS_B_MILLION](../.github/workflows/satellite-archive.yml#L270), [R2_PROJECTED_30D_STANDARD_GB_MONTH](../.github/workflows/satellite-archive.yml#L268), [R2_SATELLITE_ENDPOINT](../.github/workflows/satellite-archive.yml#L301), [R2_SATELLITE_RESERVED_30D_CLASS_A_MILLION](../.github/workflows/satellite-archive.yml#L271), [R2_SATELLITE_RESERVED_30D_CLASS_B_MILLION](../.github/workflows/satellite-archive.yml#L272), [SATELLITE_ARCHIVE_ATMOS_SHA](../.github/workflows/satellite-archive.yml#L246), [SATELLITE_ARCHIVE_BUDGET_END_DATE](../.github/workflows/satellite-archive.yml#L263), [SATELLITE_ARCHIVE_BUDGET_SCOPE](../.github/workflows/satellite-archive.yml#L261), [SATELLITE_ARCHIVE_ENABLED](../.github/workflows/satellite-archive.yml#L238), [SATELLITE_ARCHIVE_EXISTING_BYTES](../.github/workflows/satellite-archive.yml#L264), [SATELLITE_ARCHIVE_ROLLING_YEAR_ENABLED](../.github/workflows/satellite-archive.yml#L350), [SATELLITE_ARCHIVE_STORM_PILOT_ENABLED](../.github/workflows/satellite-archive.yml#L350).


## scheduler-ci

[.github/workflows/scheduler-ci.yml](../.github/workflows/scheduler-ci.yml#L1) · <code>WeatherX scheduler CI</code>

Declared triggers: [pull_request](../.github/workflows/scheduler-ci.yml#L4); [push](../.github/workflows/scheduler-ci.yml#L6) <code>{"branches":["main"],"paths":[".github/workflows/**","ops/workflows.json","docs/WORKFLOWS.md","docs/WORKFLOW_OPERATIONS.md","README.md",".github/workflows/bake.yml",".github/workflows/collect-core-model.yml",".github/workflows/collect-regional-model.yml",".github/workflows/publish-current-model-production.yml",".github/workflows/resume-model-publication.yml",".github/workflows/catalog-bake.yml",".github/workflows/scheduler-ci.yml",".github/workflows/source-checkout-probe.yml",".github/workflows/scheduler-deploy.yml",".github/workflows/satellite-archive.yml",".github/workflows/ui-release.yml",".github/workflows/ui-staging.yml",".github/workflows/ui-staging-tc.yml",".github/workflows/staging-follow-master.yml",".github/workflows/staging-data.yml",".github/workflows/staging-current-selection.yml",".github/workflows/model-inputs.yml",".github/workflows/nam-hi-diagnostic.yml",".github/workflows/staging-model-components.yml",".github/workflows/staging-model-selection.yml",".github/workflows/staging-consumer-refresh.yml",".github/workflows/staging-search.yml",".github/workflows/staging-tc-guidance.yml",".github/workflows/staging-place-renewal.yml",".github/workflows/staging-wind100.yml",".github/workflows/staging-wind100-recurring.yml",".github/workflows/staging-wind100-preflight.yml",".github/workflows/staging-search-reader.yml",".github/workflows/staging-data-activate.yml",".github/workflows/verify-backfill.yml",".github/workflows/consumer-refresh.yml",".github/workflows/data-reader-refresh.yml",".github/workflows/gdacs-feed-release.yml",".github/workflows/fusion-*.yml","scheduler/**","tests/**","tools/**","staging-controller/**"]}</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [scheduler](../.github/workflows/scheduler-ci.yml#L57) ← no needs | <code>ubuntu-latest</code> | not declared | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [scheduler / step 1](../.github/workflows/scheduler-ci.yml#L60) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## scheduler-deploy

[.github/workflows/scheduler-deploy.yml](../.github/workflows/scheduler-deploy.yml#L1) · <code>WeatherX scheduler deploy</code>

Declared triggers: [push](../.github/workflows/scheduler-deploy.yml#L5) <code>{"branches":["main"],"paths":[".github/workflows/catalog-bake.yml",".github/workflows/scheduler-deploy.yml","scheduler/**"]}</code>; [workflow_dispatch](../.github/workflows/scheduler-deploy.yml#L10).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-model-scheduler-production","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [changes](../.github/workflows/scheduler-deploy.yml#L23) ← no needs | <code>ubuntu-latest</code> | not declared | <code>5</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [deploy](../.github/workflows/scheduler-deploy.yml#L35) ← <code>changes</code> | <code>ubuntu-latest</code> | <code>production</code> | <code>15</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [deploy](../.github/workflows/scheduler-deploy.yml#L36): <code>${{ needs.changes.outputs.deploy == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [changes / step 1](../.github/workflows/scheduler-deploy.yml#L28) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [deploy / step 1](../.github/workflows/scheduler-deploy.yml#L41) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.


## source-checkout-probe

[.github/workflows/source-checkout-probe.yml](../.github/workflows/source-checkout-probe.yml#L1) · <code>WeatherX isolated source checkout probe</code>

Declared triggers: [workflow_dispatch](../.github/workflows/source-checkout-probe.yml#L5); input names <code>["atmos_sha"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-isolated-source-checkout-probe","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [admit](../.github/workflows/source-checkout-probe.yml#L20) ← no needs | <code>ubuntu-latest</code> | not declared | <code>3</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |
| [source-checkout](../.github/workflows/source-checkout-probe.yml#L42) ← <code>admit</code> | <code>ubuntu-latest</code> | <code>{"name":"atmos-source-read-ui"}</code> | <code>5</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [admit](../.github/workflows/source-checkout-probe.yml#L20): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>
- [source-checkout](../.github/workflows/source-checkout-probe.yml#L43): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [admit / step 1](../.github/workflows/source-checkout-probe.yml#L26) | caller repository (implicit) | <code>${{ github.sha }}</code> |
| [source-checkout / checkout fixed UI controller metadata only](../.github/workflows/source-checkout-probe.yml#L56) | <code>weatherx-hq/atmos</code> | <code>25c402db5149daa018e349a34a4beeba1f2dca45</code> |
| [source-checkout / checkout current master metadata only](../.github/workflows/source-checkout-probe.yml#L69) | <code>weatherx-hq/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): [SOURCE_CHECKOUT_PROBE_ENABLED](../.github/workflows/source-checkout-probe.yml#L35).


## staging-consumer-refresh

[.github/workflows/staging-consumer-refresh.yml](../.github/workflows/staging-consumer-refresh.yml#L1) · <code>WeatherX staging public consumer repair</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-consumer-refresh.yml#L4); input names <code>["mode","confirm"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-publication","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [refresh](../.github/workflows/staging-consumer-refresh.yml#L22) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [refresh / step 1](../.github/workflows/staging-consumer-refresh.yml#L36) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [refresh / Checkout only the fixed already-qualified staging consumer](../.github/workflows/staging-consumer-refresh.yml#L46) | <code>Andrewegao/atmos</code> | <code>0aa9fbed9e179ab2ccb6ac456727b9f33124ddb6</code> |

Variable references (declared names only; values and activation unknown): [STAGING_CONSUMER_APPROVED_SETTINGS_SHA256](../.github/workflows/staging-consumer-refresh.yml#L34), [STAGING_CONSUMER_APPROVED_VERSION](../.github/workflows/staging-consumer-refresh.yml#L33), [STAGING_CONSUMER_ENABLED](../.github/workflows/staging-consumer-refresh.yml#L30), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-consumer-refresh.yml#L32).


## staging-current-selection

[.github/workflows/staging-current-selection.yml](../.github/workflows/staging-current-selection.yml#L1) · <code>WeatherX current published staging source selection</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-current-selection.yml#L3).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-current-selection","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [select](../.github/workflows/staging-current-selection.yml#L11) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [select / step 1](../.github/workflows/staging-current-selection.yml#L17) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [select / Checkout pinned schema validator only](../.github/workflows/staging-current-selection.yml#L21) | <code>Andrewegao/atmos</code> | <code>dbc97a26bc239398ffa9ec157a094148961b6451</code> |

Variable references (declared names only; values and activation unknown): none detected.


## staging-data-activate

[.github/workflows/staging-data-activate.yml](../.github/workflows/staging-data-activate.yml#L1) · <code>WeatherX shared data staging activation</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-data-activate.yml#L4); input names <code>["release_id","release_manifest_sha256","catalog_id","catalog_sha256"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-publication","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [activate](../.github/workflows/staging-data-activate.yml#L28) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>90</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [activate / step 1](../.github/workflows/staging-data-activate.yml#L45) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [activate / Checkout fixed qualified consumer and schema validators](../.github/workflows/staging-data-activate.yml#L53) | <code>Andrewegao/atmos</code> | <code>a58eff158b56ef2ba25189d2b859315b00893a14</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ACTIVATION_ENABLED](../.github/workflows/staging-data-activate.yml#L36), [STAGING_DATA_APPROVED_RECEIPT_SHA256](../.github/workflows/staging-data-activate.yml#L38), [STAGING_DATA_APPROVED_SELECTION_SHA256](../.github/workflows/staging-data-activate.yml#L37), [STAGING_DATA_ENABLED](../.github/workflows/staging-data-activate.yml#L34), [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-data-activate.yml#L35), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-data-activate.yml#L39).


## staging-data

[.github/workflows/staging-data.yml](../.github/workflows/staging-data.yml#L1) · <code>WeatherX shared data staging preparation</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-data.yml#L5); input names <code>["release_id","release_manifest_sha256","catalog_id","catalog_sha256"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-shared-staging-preparation","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [prepare](../.github/workflows/staging-data.yml#L29) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>90</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [prepare / step 1](../.github/workflows/staging-data.yml#L44) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [prepare / Checkout pinned schema validator only (not a collector or candidate source)](../.github/workflows/staging-data.yml#L50) | <code>Andrewegao/atmos</code> | <code>dbc97a26bc239398ffa9ec157a094148961b6451</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_APPROVED_SELECTION_SHA256](../.github/workflows/staging-data.yml#L37), [STAGING_DATA_ENABLED](../.github/workflows/staging-data.yml#L35), [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-data.yml#L36), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-data.yml#L38).


## staging-follow-master

[.github/workflows/staging-follow-master.yml](../.github/workflows/staging-follow-master.yml#L1) · <code>WeatherX follow master on staging only</code>

Declared triggers: [schedule](../.github/workflows/staging-follow-master.yml#L4) <code>["3,18,33,48 * * * *"]</code>; [workflow_dispatch](../.github/workflows/staging-follow-master.yml#L5).

Workflow permissions: <code>{"contents":"read","actions":"write"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-follow-master","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [follow](../.github/workflows/staging-follow-master.yml#L14) ← no needs | <code>ubuntu-latest</code> | <code>staging</code> | <code>5</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [follow](../.github/workflows/staging-follow-master.yml#L14): <code>${{ vars.UI_STAGING_FOLLOW_MASTER_ENABLED == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [follow / step 1](../.github/workflows/staging-follow-master.yml#L19) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [follow / Read master identity only, never execute candidate code](../.github/workflows/staging-follow-master.yml#L23) | <code>Andrewegao/atmos</code> | <code>master</code> |

Variable references (declared names only; values and activation unknown): [UI_STAGING_FOLLOW_MASTER_ENABLED](../.github/workflows/staging-follow-master.yml#L14).


## staging-model-components

[.github/workflows/staging-model-components.yml](../.github/workflows/staging-model-components.yml#L1) · <code>WeatherX staging model display qualification</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-model-components.yml#L5); input names <code>["source_sha","validator_sha","model","init","archive_run_id","archive_attempt","archive_receipt_sha256","archive_marker_sha256","prior_catalog_id","prior_catalog_sha256"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-model-components-${{ inputs.model }}","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [qualify](../.github/workflows/staging-model-components.yml#L54) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>90</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [qualify / step 1](../.github/workflows/staging-model-components.yml#L79) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [qualify / Checkout approved scientific validator with full source ancestry](../.github/workflows/staging-model-components.yml#L85) | <code>Andrewegao/atmos</code> | <code>${{ inputs.validator_sha }}</code> |
| [qualify / Checkout unchanged staging consumer for actual schema validation](../.github/workflows/staging-model-components.yml#L94) | <code>Andrewegao/atmos</code> | <code>a58eff158b56ef2ba25189d2b859315b00893a14</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-model-components.yml#L60), [STAGING_MODEL_APPROVED_SOURCE_SHA](../.github/workflows/staging-model-components.yml#L63), [STAGING_MODEL_COMPONENTS_APPROVED_REQUESTS_JSON](../.github/workflows/staging-model-components.yml#L62), [STAGING_MODEL_COMPONENTS_ENABLED](../.github/workflows/staging-model-components.yml#L61), [STAGING_MODEL_VALIDATOR_SOURCE_SHA](../.github/workflows/staging-model-components.yml#L64), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-model-components.yml#L65).


## staging-model-selection

[.github/workflows/staging-model-selection.yml](../.github/workflows/staging-model-selection.yml#L1) · <code>WeatherX staging model selection assembly</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-model-selection.yml#L5); input names <code>["icon_catalog_id","hrrr_ak_catalog_id","hrdps_catalog_id","nam_catalog_id","nam_hi_catalog_id","nam_ak_catalog_id","arome_antilles_catalog_id"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-model-selection","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [assemble](../.github/workflows/staging-model-selection.yml#L41) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [assemble / step 1](../.github/workflows/staging-model-selection.yml#L61) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-model-selection.yml#L47), [STAGING_MODEL_APPROVED_SOURCE_SHA](../.github/workflows/staging-model-selection.yml#L50), [STAGING_MODEL_SELECTION_APPROVED_CATALOGS_JSON](../.github/workflows/staging-model-selection.yml#L49), [STAGING_MODEL_SELECTION_ENABLED](../.github/workflows/staging-model-selection.yml#L48), [STAGING_MODEL_VALIDATOR_SOURCE_SHA](../.github/workflows/staging-model-selection.yml#L51), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-model-selection.yml#L52).


## staging-place-renewal

[.github/workflows/staging-place-renewal.yml](../.github/workflows/staging-place-renewal.yml#L1) · <code>WeatherX staging place renewal</code>

Declared triggers: [schedule](../.github/workflows/staging-place-renewal.yml#L4) <code>["37 1,7,13,19 * * *","47 5,17 * * *"]</code>; [workflow_dispatch](../.github/workflows/staging-place-renewal.yml#L7); input names <code>["family"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-publication","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [renew](../.github/workflows/staging-place-renewal.yml#L21) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>75</code> | no job group; workflow-wide limit still applies if declared | <code>{"fail-fast":false,"max-parallel":3,"matrix":{"family":["surf","paragliding","tides"]}}</code> | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [renew](../.github/workflows/staging-place-renewal.yml#L21): <code>vars.STAGING_PLACES_RENEWAL_ENABLED == 'true' &#124;&#124; github.event_name == 'workflow_dispatch'</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [renew / step 1](../.github/workflows/staging-place-renewal.yml#L42) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [renew / Checkout exact private producer and consumer without retained credentials](../.github/workflows/staging-place-renewal.yml#L52) | <code>weatherx-hq/atmos</code> | <code>${{ env.ATMOS_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-place-renewal.yml#L39), [STAGING_PLACES_RENEWAL_ATMOS_SHA](../.github/workflows/staging-place-renewal.yml#L36), [STAGING_PLACES_RENEWAL_CONTROLLER_SHA256](../.github/workflows/staging-place-renewal.yml#L38), [STAGING_PLACES_RENEWAL_ENABLED](../.github/workflows/staging-place-renewal.yml#L21), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-place-renewal.yml#L40).


## staging-places

[.github/workflows/staging-places.yml](../.github/workflows/staging-places.yml#L1) · <code>WeatherX staging place seed</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-places.yml#L5); input names <code>["action","family","atmos_sha","seed_tag","seed_sha256","plaintext_sha256","manifest_sha256","completion_sha256","expected_pointer_sha256"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-publication","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [places](../.github/workflows/staging-places.yml#L50) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>60</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [places / step 1](../.github/workflows/staging-places.yml#L78) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [places / Checkout reviewed producer and consumer with no retained Git credentials](../.github/workflows/staging-places.yml#L95) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.atmos_sha }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-places.yml#L66), [STAGING_PLACES_APPROVED_ATMOS_SHA](../.github/workflows/staging-places.yml#L69), [STAGING_PLACES_APPROVED_COMPLETION_SHA256](../.github/workflows/staging-places.yml#L74), [STAGING_PLACES_APPROVED_FAMILY](../.github/workflows/staging-places.yml#L70), [STAGING_PLACES_APPROVED_MANIFEST_SHA256](../.github/workflows/staging-places.yml#L73), [STAGING_PLACES_APPROVED_PLAINTEXT_SHA256](../.github/workflows/staging-places.yml#L72), [STAGING_PLACES_APPROVED_POINTER_SHA256](../.github/workflows/staging-places.yml#L75), [STAGING_PLACES_APPROVED_QUALIFIER_SHA256](../.github/workflows/staging-places.yml#L76), [STAGING_PLACES_APPROVED_SEED_SHA256](../.github/workflows/staging-places.yml#L71), [STAGING_PLACES_APPROVED_WORKFLOW_SHA](../.github/workflows/staging-places.yml#L68), [STAGING_PLACES_ENABLED](../.github/workflows/staging-places.yml#L65), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-places.yml#L67).


## staging-search-reader

[.github/workflows/staging-search-reader.yml](../.github/workflows/staging-search-reader.yml#L1) · <code>WeatherX staging search reader code-only qualification</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-search-reader.yml#L4); input names <code>["action","source_sha","exclusive_window"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-publication","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [reader](../.github/workflows/staging-search-reader.yml#L26) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>30</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [reader / step 1](../.github/workflows/staging-search-reader.yml#L46) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [reader / step 5](../.github/workflows/staging-search-reader.yml#L54) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.source_sha }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-search-reader.yml#L44), [STAGING_SEARCH_READER_APPROVED_BOUNDARY_SHA256](../.github/workflows/staging-search-reader.yml#L38), [STAGING_SEARCH_READER_APPROVED_SOURCE_SHA](../.github/workflows/staging-search-reader.yml#L36), [STAGING_SEARCH_READER_APPROVED_VERSION](../.github/workflows/staging-search-reader.yml#L37), [STAGING_SEARCH_READER_ENABLED](../.github/workflows/staging-search-reader.yml#L35), [STAGING_WIND100_READER_CATALOG_ID](../.github/workflows/staging-search-reader.yml#L41), [STAGING_WIND100_READER_DYNAMIC](../.github/workflows/staging-search-reader.yml#L40), [STAGING_WIND100_READER_ENABLED](../.github/workflows/staging-search-reader.yml#L39), [STAGING_WIND100_READER_RUN_ID](../.github/workflows/staging-search-reader.yml#L42), [STAGING_WIND100_READER_SELECTION_SHA256](../.github/workflows/staging-search-reader.yml#L43).


## staging-search

[.github/workflows/staging-search.yml](../.github/workflows/staging-search.yml#L1) · <code>WeatherX staging search candidate</code>

Declared triggers: [schedule](../.github/workflows/staging-search.yml#L5) <code>["17 */6 * * *"]</code>; [workflow_dispatch](../.github/workflows/staging-search.yml#L7); input names <code>["action","source_release","candidate_sha256","expected_pointer_sha256"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-search","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [search](../.github/workflows/staging-search.yml#L32) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>15</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [search](../.github/workflows/staging-search.yml#L32): <code>github.event_name != 'schedule' &#124;&#124; vars.STAGING_SEARCH_SCHEDULE_ENABLED == 'true'</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [search / step 1](../.github/workflows/staging-search.yml#L51) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [search / Checkout the fixed producer or exact reviewed UI source](../.github/workflows/staging-search.yml#L63) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.action == 'prepare' &amp;&amp; 'dfa25e9f473f15d5e2f630fe78c268b73234bd3a' &#124;&#124; vars.STAGING_SEARCH_V4_APPROVED_UI_SOURCE_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-search.yml#L48), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-search.yml#L49), [STAGING_SEARCH_APPROVED_CANDIDATE_SHA256](../.github/workflows/staging-search.yml#L45), [STAGING_SEARCH_ENABLED](../.github/workflows/staging-search.yml#L43), [STAGING_SEARCH_RENEWAL_ENABLED](../.github/workflows/staging-search.yml#L44), [STAGING_SEARCH_SCHEDULE_ENABLED](../.github/workflows/staging-search.yml#L32), [STAGING_SEARCH_V4_APPROVED_RELEASE_ID](../.github/workflows/staging-search.yml#L47), [STAGING_SEARCH_V4_APPROVED_UI_SOURCE_SHA](../.github/workflows/staging-search.yml#L46).


## staging-shared-read-pin

[.github/workflows/staging-shared-read-pin.yml](../.github/workflows/staging-shared-read-pin.yml#L1) · <code>WeatherX staging shared-read canary pin</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-shared-read-pin.yml#L7); input names <code>["action","release_id","catalog_id","hours","reason"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-shared-read-pin","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [pin](../.github/workflows/staging-shared-read-pin.yml#L37) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [pin / step 1](../.github/workflows/staging-shared-read-pin.yml#L51) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-shared-read-pin.yml#L43), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-shared-read-pin.yml#L45), [STAGING_SHARED_READ_PIN_ENABLED](../.github/workflows/staging-shared-read-pin.yml#L44).


## staging-shared-read-probe

[.github/workflows/staging-shared-read-probe.yml](../.github/workflows/staging-shared-read-probe.yml#L1) · <code>WeatherX staging shared-read follow probe</code>

Declared triggers: [schedule](../.github/workflows/staging-shared-read-probe.yml#L6) <code>["25,55 * * * *"]</code>; [workflow_dispatch](../.github/workflows/staging-shared-read-probe.yml#L7).

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-shared-read-probe","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [probe](../.github/workflows/staging-shared-read-probe.yml#L15) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>10</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [probe](../.github/workflows/staging-shared-read-probe.yml#L15): <code>${{ github.event_name == 'workflow_dispatch' &#124;&#124; vars.STAGING_SHARED_READ_PROBE_ENABLED == 'true' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [probe / step 1](../.github/workflows/staging-shared-read-probe.yml#L22) | caller repository (implicit) | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): [STAGING_SHARED_READ_PROBE_ENABLED](../.github/workflows/staging-shared-read-probe.yml#L15).


## staging-tc-guidance

[.github/workflows/staging-tc-guidance.yml](../.github/workflows/staging-tc-guidance.yml#L1) · <code>WeatherX isolated staging cyclone guidance</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-tc-guidance.yml#L5); input names <code>["source_sha"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-tc-guidance","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [publish](../.github/workflows/staging-tc-guidance.yml#L17) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>20</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [publish / step 1](../.github/workflows/staging-tc-guidance.yml#L31) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [publish / Checkout qualified producer and publication validator](../.github/workflows/staging-tc-guidance.yml#L39) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.source_sha }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-tc-guidance.yml#L26), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-tc-guidance.yml#L27), [STAGING_TC_APPROVED_SOURCE_SHA](../.github/workflows/staging-tc-guidance.yml#L24), [STAGING_TC_GUIDANCE_ENABLED](../.github/workflows/staging-tc-guidance.yml#L25).


## staging-wind100-preflight

[.github/workflows/staging-wind100-preflight.yml](../.github/workflows/staging-wind100-preflight.yml#L1) · <code>Check recurring staging Wind100 credentials</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-wind100-preflight.yml#L4).

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: not declared.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [preflight](../.github/workflows/staging-wind100-preflight.yml#L12) ← no needs | <code>./.github/workflows/staging-wind100-recurring.yml</code> | not declared | not declared | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [preflight](../.github/workflows/staging-wind100-preflight.yml#L12): <code>${{ github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| No direct checkout; inspect linked reusable jobs | — | — |

Variable references (declared names only; values and activation unknown): none detected.


## staging-wind100-recurring

[.github/workflows/staging-wind100-recurring.yml](../.github/workflows/staging-wind100-recurring.yml#L1) · <code>publish recurring staging ECMWF native 100m wind</code>

Declared triggers: [workflow_call](../.github/workflows/staging-wind100-recurring.yml#L4); input names <code>["check_only"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-native-wind100-recurring","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [wind100](../.github/workflows/staging-wind100-recurring.yml#L29) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>90</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [wind100 / step 3](../.github/workflows/staging-wind100-recurring.yml#L67) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [wind100 / Checkout the exact ordinary core producer](../.github/workflows/staging-wind100-recurring.yml#L73) | <code>weatherx-hq/atmos</code> | <code>7a50f19714f22e04dc610a5aa33d311b7d1dc673</code> |
| [wind100 / Checkout the exact reviewed point-only augmenter and publisher](../.github/workflows/staging-wind100-recurring.yml#L82) | <code>weatherx-hq/atmos</code> | <code>9174329db6ca8527569e67f14ef70406dedefb69</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-wind100-recurring.yml#L40), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-wind100-recurring.yml#L43), [STAGING_WIND100_CONTROLLER_SHA256](../.github/workflows/staging-wind100-recurring.yml#L42), [STAGING_WIND100_ENABLED](../.github/workflows/staging-wind100-recurring.yml#L41).


## staging-wind100

[.github/workflows/staging-wind100.yml](../.github/workflows/staging-wind100.yml#L1) · <code>WeatherX isolated staging native 100m wind</code>

Declared triggers: [workflow_dispatch](../.github/workflows/staging-wind100.yml#L4); input names <code>["confirmation"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-staging-native-wind100","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [wind100](../.github/workflows/staging-wind100.yml#L16) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"data-staging","url":"https://staging.weatherx.org"}</code> | <code>210</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [wind100 / step 1](../.github/workflows/staging-wind100.yml#L32) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [wind100 / Checkout the exact reviewed native 100m producer](../.github/workflows/staging-wind100.yml#L40) | <code>weatherx-hq/atmos</code> | <code>${{ env.ATMOS_SHA }}</code> |
| [wind100 / Checkout an independent clean validator and publisher copy](../.github/workflows/staging-wind100.yml#L48) | <code>weatherx-hq/atmos</code> | <code>${{ env.ATMOS_SHA }}</code> |

Variable references (declared names only; values and activation unknown): [STAGING_DATA_ISOLATION_APPROVED](../.github/workflows/staging-wind100.yml#L25), [STAGING_R2_ACCOUNT_ID](../.github/workflows/staging-wind100.yml#L28), [STAGING_WIND100_ATMOS_SHA](../.github/workflows/staging-wind100.yml#L22), [STAGING_WIND100_CONTROLLER_SHA256](../.github/workflows/staging-wind100.yml#L27), [STAGING_WIND100_ENABLED](../.github/workflows/staging-wind100.yml#L26).


## ui-release

[.github/workflows/ui-release.yml](../.github/workflows/ui-release.yml#L1) · <code>WeatherX UI production promotion</code>

Declared triggers: [workflow_dispatch](../.github/workflows/ui-release.yml#L5); input names <code>["staging_run_id","atmos_sha","candidate_digest"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read","issues":"write"}</code>. Workflow concurrency: <code>{"group":"weatherx-ui-production","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [promote](../.github/workflows/ui-release.yml#L27) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"ui-production","url":"https://weatherx.org"}</code> | <code>45</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [promote / step 1](../.github/workflows/ui-release.yml#L40) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [promote / checkout reviewed release controller (not candidate source)](../.github/workflows/ui-release.yml#L42) | <code>weatherx-hq/atmos</code> | <code>25c402db5149daa018e349a34a4beeba1f2dca45</code> |

Variable references (declared names only; values and activation unknown): [UI_DEPLOYMENT_HOLD_UNTIL](../.github/workflows/ui-release.yml#L35), [UI_ISOLATION_APPROVED](../.github/workflows/ui-release.yml#L34), [UI_PAGES_CONFIG_SHA256](../.github/workflows/ui-release.yml#L71), [UI_RELEASES_ENABLED](../.github/workflows/ui-release.yml#L33).


## ui-staging-tc

[.github/workflows/ui-staging-tc.yml](../.github/workflows/ui-staging-tc.yml#L1) · <code>WeatherX isolated TC UI qualification</code>

Declared triggers: [workflow_dispatch](../.github/workflows/ui-staging-tc.yml#L7); input names <code>["atmos_sha"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"weatherx-ui-tc-build","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [build](../.github/workflows/ui-staging-tc.yml#L19) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"atmos-source-read-ui"}</code> | <code>60</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [build](../.github/workflows/ui-staging-tc.yml#L19): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [build / step 2](../.github/workflows/ui-staging-tc.yml#L43) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [build / checkout exact reviewed TC release controller](../.github/workflows/ui-staging-tc.yml#L45) | <code>weatherx-hq/atmos</code> | <code>f23664837274fc06a4a94b9bb0fc8d7fa1ee8c58</code> |
| [build / checkout exact candidate Atmos source](../.github/workflows/ui-staging-tc.yml#L53) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.atmos_sha }}</code> |

Variable references (declared names only; values and activation unknown): [UI_BUILDS_ENABLED](../.github/workflows/ui-staging-tc.yml#L25), [UI_BUILD_PUBLIC_KEY](../.github/workflows/ui-staging-tc.yml#L26), [UI_STAGING_CORE_PROFILE_APPROVED](../.github/workflows/ui-staging-tc.yml#L27), [UI_STAGING_TC_PROFILE_APPROVED](../.github/workflows/ui-staging-tc.yml#L28).


## ui-staging

[.github/workflows/ui-staging.yml](../.github/workflows/ui-staging.yml#L1) · <code>WeatherX UI staging qualification</code>

Declared triggers: [workflow_dispatch](../.github/workflows/ui-staging.yml#L6); input names <code>["atmos_sha","model_selection_sha256"]</code>.

Workflow permissions: <code>{"contents":"read","actions":"read","issues":"write"}</code>. Workflow concurrency: <code>{"group":"weatherx-ui-staging","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [profile](../.github/workflows/ui-staging.yml#L27) ← no needs | <code>ubuntu-latest</code> | <code>{"name":"ui-staging"}</code> | <code>5</code> | no job group; workflow-wide limit still applies if declared | not declared | <code>{"contents":"read"}</code> |
| [build](../.github/workflows/ui-staging.yml#L69) ← <code>profile</code> | <code>ubuntu-latest</code> | <code>{"name":"atmos-source-read-ui"}</code> | <code>60</code> | no job group; workflow-wide limit still applies if declared | not declared | <code>{"contents":"read"}</code> |
| [qualify](../.github/workflows/ui-staging.yml#L174) ← <code>["profile","build"]</code> | <code>ubuntu-latest</code> | <code>{"name":"ui-staging","url":"https://staging.weatherx.org"}</code> | <code>45</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Declared job conditions (additional step/helper checks may apply):

- [build](../.github/workflows/ui-staging.yml#L70): <code>${{ github.event_name == 'workflow_dispatch' &amp;&amp; github.ref == 'refs/heads/main' }}</code>

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [profile / step 1](../.github/workflows/ui-staging.yml#L41) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [build / step 2](../.github/workflows/ui-staging.yml#L102) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [build / checkout reviewed release controller](../.github/workflows/ui-staging.yml#L104) | <code>weatherx-hq/atmos</code> | <code>${{ needs.profile.outputs.model_selection_sha256 == 'none' &amp;&amp; '25c402db5149daa018e349a34a4beeba1f2dca45' &#124;&#124; 'fa857448dafa0f54eafdd63999b85eca4916edc8' }}</code> |
| [build / checkout exact candidate Atmos source](../.github/workflows/ui-staging.yml#L127) | <code>weatherx-hq/atmos</code> | <code>${{ inputs.atmos_sha }}</code> |
| [qualify / step 1](../.github/workflows/ui-staging.yml#L197) | caller repository (implicit) | implicit event/default ref; no explicit pin here |
| [qualify / checkout pinned controller, NOT candidate source](../.github/workflows/ui-staging.yml#L199) | <code>weatherx-hq/atmos</code> | <code>${{ needs.profile.outputs.model_selection_sha256 == 'none' &amp;&amp; '25c402db5149daa018e349a34a4beeba1f2dca45' &#124;&#124; 'fa857448dafa0f54eafdd63999b85eca4916edc8' }}</code> |

Variable references (declared names only; values and activation unknown): [FUSION_CALIBRATION_RUNTIME_ENABLED](../.github/workflows/ui-staging.yml#L87), [STAGING_WIND100_UI_CATALOG_ID](../.github/workflows/ui-staging.yml#L54), [STAGING_WIND100_UI_DYNAMIC](../.github/workflows/ui-staging.yml#L57), [STAGING_WIND100_UI_ENABLED](../.github/workflows/ui-staging.yml#L53), [STAGING_WIND100_UI_RUN_ID](../.github/workflows/ui-staging.yml#L55), [STAGING_WIND100_UI_SELECTION_SHA256](../.github/workflows/ui-staging.yml#L56), [UI_BUILDS_ENABLED](../.github/workflows/ui-staging.yml#L78), [UI_BUILD_PUBLIC_KEY](../.github/workflows/ui-staging.yml#L79), [UI_DEPLOYMENT_HOLD_UNTIL](../.github/workflows/ui-staging.yml#L184), [UI_ISOLATION_APPROVED](../.github/workflows/ui-staging.yml#L183), [UI_PAGES_CONFIG_SHA256](../.github/workflows/ui-staging.yml#L228), [UI_RELEASES_ENABLED](../.github/workflows/ui-staging.yml#L182), [UI_STAGING_ACCOUNT_ID](../.github/workflows/ui-staging.yml#L227), [UI_STAGING_ACCOUNT_PROFILE_APPROVED](../.github/workflows/ui-staging.yml#L52), [UI_STAGING_CORE_PROFILE_APPROVED](../.github/workflows/ui-staging.yml#L50), [UI_STAGING_MODEL_SELECTION_APPROVED_SHA256](../.github/workflows/ui-staging.yml#L49), [UI_STAGING_STATIC_COMPRESSION_APPROVED](../.github/workflows/ui-staging.yml#L51).


## verify-backfill

[.github/workflows/verify-backfill.yml](../.github/workflows/verify-backfill.yml#L1) · <code>verify-backfill</code>

Declared triggers: [workflow_dispatch](../.github/workflows/verify-backfill.yml#L5); input names <code>["half","days"]</code>.

Workflow permissions: <code>{"contents":"read"}</code>. Workflow concurrency: <code>{"group":"${{ inputs.half == 'full' &amp;&amp; 'bake' &#124;&#124; format('verify-backfill-{0}', inputs.half) }}","cancel-in-progress":false}</code>.

| Job / dependency graph | Runner or reusable workflow | Environment | Timeout (minutes) | Concurrency | Matrix / parallelism | Permissions |
| --- | --- | --- | --- | --- | --- | --- |
| [backfill](../.github/workflows/verify-backfill.yml#L27) ← no needs | <code>ubuntu-latest</code> | <code>production</code> | <code>360</code> | no job group; workflow-wide limit still applies if declared | not declared | inherits workflow/default policy |

Checkout declarations (not a claim of approval or checkout success):

| Job / checkout step | Repository | Ref |
| --- | --- | --- |
| [backfill / checkout atmos (private, read-only deploy key)](../.github/workflows/verify-backfill.yml#L31) | <code>Andrewegao/atmos</code> | implicit event/default ref; no explicit pin here |

Variable references (declared names only; values and activation unknown): none detected.
